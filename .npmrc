registry=https://registry.npmjs.org/
@apple:registry=https://registry.npmjs.org/
# Force all packages through public registry, overriding Apple internal mirrors.
# Without this, npm installs on the Work Mac write artifacts.apple.com URLs into
# package-lock.json, which breaks Vercel (ENOTFOUND artifacts.apple.com).
